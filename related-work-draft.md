# Related Work (draft section for the manuscript)

> Drop-in for a new §2.5 (or a standalone "Related Work" after §3). ~800 words. Citation keys `[N]` resolve to [`references.md`](./references.md); 4–5 entries there are flagged ⚠ verify and must be confirmed before submission. Systems are cited by their canonical documentation; this section's job is to position OpenWOP, not to re-survey each field.

OpenWOP sits at the intersection of four bodies of work. The contribution is not a new execution engine but a **vendor-neutral wire contract** for the orchestration layer; related work is therefore organized by what OpenWOP deliberately does *not* duplicate.

**Durable-execution runtimes.** Temporal, Cadence, Restate, DBOS, AWS Step Functions, and Azure Durable Functions provide durable, replayable, long-running execution with deterministic recovery [6, 8, 9]. They solve *execution* — the same problem an OpenWOP **host** solves internally — but each exposes a *proprietary, runtime-specific* surface: workflow definitions, event histories, and resume semantics are bound to the vendor SDK and cannot move across runtimes. OpenWOP's claim is orthogonal: it standardizes the *client↔host boundary* (run creation, lifecycle events, interrupts, artifacts, replay/debug, capability discovery, conformance) so that durable execution becomes *portable* across independently-implemented hosts, of which Temporal-class engines could be one. The distinction is the same as "a database engine" vs. "the SQL wire protocol."

**Vendor-neutral workflow standards — and why portability did not follow.** OpenWOP is not the first attempt at a portable workflow standard. WS-BPEL, XPDL, BPMN (and its executable subset), and more recently the CNCF **Serverless Workflow** specification and **Argo Workflows** all aimed at portable, declarative workflow definitions [12, 15, 16] (see also [13, 14]). The historical lesson is sobering and directly relevant: a portable *definition format* did not yield portable *execution*, because the standards specified the static workflow graph but left lifecycle, event semantics, human-checkpoint behavior, replay, and capability negotiation to each engine. OpenWOP's wager is that **portability lives in the runtime contract, not the definition language** — hence its kernel centers the `Run`, the `EventLog`, `Interrupt/ResumePayload`, and a `ConformanceProfile` with scenario evidence, rather than only a `WorkflowDefinition`. The paper should make this contrast explicit: it is the sharpest articulation of why OpenWOP might succeed where definition-only standards stalled, and reviewers familiar with BPMN's portability gap will look for exactly this argument.

**Agent protocols.** Two recent protocols are adjacent and complementary rather than competing. The Model Context Protocol (MCP) standardizes how an application connects to tools, resources, prompts, and context [17]; Agent2Agent (A2A) standardizes inter-agent discovery and delegation [18]. Agent-framework orchestration (LangGraph, AutoGen, CrewAI) embeds coordination logic inside a single runtime [19, 20, 21]. OpenWOP occupies the layer none of these own: durable, observable, replayable, portable *workflow control* across hosts. The manuscript's §5 layering (A2A horizontal, MCP vertical, OpenWOP durable-control) is the right framing and should cite the protocols' primary specifications rather than only the project's own comparison document — the latter is a steward source and cannot carry the neutrality of the claim alone.

**LLM-native workflow orchestration, and the protocol proliferation.** A distinct and very recent line of work treats workflow orchestration itself as an LLM capability — systems that generate, optimize, or compile agentic workflows: DSPy's declarative, self-improving LM pipelines [26], AFlow's automated workflow-graph search [28], and WorkflowLLM, which fine-tunes orchestration ability and frames the shift from Robotic Process Automation to "Agentic Process Automation" [27]; the space is now mapped by dedicated surveys from static templates to dynamic runtime graphs [34]. In parallel, a wave of agent-communication protocols (MCP, A2A, the Agent Communication Protocol, the Agent Network Protocol) has appeared and is already being catalogued by dedicated protocol surveys [31, 32] and broad multi-agent-systems surveys [29, 30]. These efforts are complementary to OpenWOP and *sharpen its gap rather than fill it*: workflow-generation systems optimize the workflow graph but assume a runtime to execute it; agent protocols standardize how agents discover and message one another, not how a multi-step run is durably executed, observed, interrupted, replayed, and certified across hosts. The very proliferation of competing agent protocols is itself evidence of the orchestration-layer fragmentation this paper names. The emerging agent-reliability literature — e.g., Byzantine-fault-tolerance perspectives on multi-agent systems [33] — further motivates the durable-execution and replay guarantees OpenWOP elevates from per-runtime engineering into a portable, conformance-tested contract. The evaluation literature is itself fragmenting [38]; a unified evaluation framework recently showed that scaffold and framework choice materially shift agent outcomes [39] — direct external evidence that agent behavior is *not* portable across implementations today, which is precisely the gap a conformance-tested wire contract is meant to close.

**Observability, replay, and event sourcing.** OpenWOP's `EventLog` + replay/fork model is an application of event sourcing and deterministic replay [22, 23], and its `TraceContext` aligns with OpenTelemetry's propagation model [24]. These are mature ideas; OpenWOP's novelty is not the mechanism but its elevation into a *cross-vendor protocol obligation* with conformance scenarios, so that an observability or replay guarantee is portable rather than per-runtime. Recent work brings record-and-replay and determinism directly to LLM agents — logging only non-deterministic events to reconstruct state [35], building deterministic LLM workflows [36], and characterizing why even temperature-zero inference drifts [37] — which is exactly why OpenWOP records non-determinism as pins in the event log rather than regenerating it at fork time.

**Standardization as the mechanism.** The paper's thesis — that a vendor-neutral protocol *accelerates* an ecosystem — has precedent worth citing as analogy rather than proof: TCP/IP, SQL, HTTP, and (recently) OpenTelemetry each decoupled an interoperability layer from competing implementations and enlarged the market [25, 40, 24]. The paper should invoke these as *motivating analogy* and explicitly mark the "acceleration" claim as a long-term hypothesis (consistent with its §9 discipline), not assert OpenWOP has achieved comparable network effects.

**Positioning summary.** Against durable-execution runtimes, OpenWOP is the *wire contract they could share*; against prior workflow standards, it standardizes the *runtime contract* they omitted; against MCP/A2A, it is the *complementary orchestration layer*; against event-sourcing/observability practice, it makes those guarantees a *portable protocol obligation*. No prior artifact offers a vendor-neutral, conformance-tested, run-centered orchestration contract spanning lifecycle, human checkpoints, replay, and capability discovery — which is the gap the paper names.

---

## References

The `[N]` keys above resolve to the bibliography in [`references.md`](./references.md) (25 entries: 5 steward primary sources clearly labeled as such + 20 external). Before submission, confirm the ⚠-flagged entries ([9], [14], [18], [23], [25]) and format all to the venue's citation style.
