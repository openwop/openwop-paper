# macOS / editor cruft
.DS_Store
*~
.*.swp

# LaTeX build intermediates (main.pdf is committed deliberately)
*.aux
*.log
*.out
*.xdv
*.fls
*.fdb_latexmk
*.synctex.gz
*.toc

# internal working docs — kept local, never published to the public repo
review/
